- Créer une base mySQL "carnet_contact_ejb" pour faire fonctionner le projet.
- Le script SQL pour générer les tables n'est pas nécessaire, 
  le fichier persistence.xml contient une directive permettant de générer les tables à partir des entités.
- Utiliser depuis le client lourd la fonctionnalité 1) qui permet de remplir la base de donnée avec des valeurs de test.
- Le client lourd Java est pleinement fonctionnel, mais le client web jsp ne l'est que partiellement.